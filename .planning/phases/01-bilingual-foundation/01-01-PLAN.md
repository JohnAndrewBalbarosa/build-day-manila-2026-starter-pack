---
phase: 01-bilingual-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/src/agent/prompt.py
autonomous: false
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - FOUND-05
  - LANG-01
  - LANG-02
  - LANG-03
  - LANG-04

must_haves:
  truths:
    - "Agent sends a preprocessed frame to Gemini 2.5 Flash and receives a text guess"
    - "Agent returns None (skips) when the LLM outputs SKIP"
    - "Agent outputs multi-word phrases (1-5 words) as guesses"
    - "Agent can produce guesses in Tagalog when the action fits Filipino context"
    - "Frames are resized to 512px max and JPEG-compressed before LLM calls"
    - "Module-level state (frame_count, guess_history, last_guess) persists across analyze() calls"
    - "System prompt is charades-specific (miming, gestures) not generic scene description"
  artifacts:
    - path: "agent/src/agent/prompt.py"
      provides: "Complete Phase 1 agent: bilingual charades prompt, frame preprocessing, Gemini integration, state tracking"
      contains: "SYSTEM_PROMPT"
      min_lines: 60
  key_links:
    - from: "agent/src/agent/prompt.py::analyze"
      to: "pydantic_ai.Agent.run"
      via: "_agent.run() with BinaryContent"
      pattern: "_agent\\.run\\("
    - from: "agent/src/agent/prompt.py::_preprocess_frame"
      to: "PIL.Image.thumbnail"
      via: "resize + JPEG compress"
      pattern: "img\\.thumbnail\\("
    - from: "agent/src/agent/prompt.py::_agent"
      to: "google-gla:gemini-2.5-flash"
      via: "pydantic-ai Agent constructor"
      pattern: "google-gla:gemini-2.5-flash"

user_setup:
  - service: gemini
    why: "Vision LLM for frame analysis"
    env_vars:
      - name: GEMINI_API_KEY
        source: "Google AI Studio -> Get API Key (https://aistudio.google.com/apikey)"
    dashboard_config: []
---

<objective>
Implement the complete Phase 1 bilingual charades agent in `agent/src/agent/prompt.py`.

Purpose: Deliver a minimum viable competitor that can watch a charades livestream, send preprocessed frames to Gemini 2.5 Flash, and submit bilingual guesses (English or Tagalog phrases) while preserving its 10-guess budget through prompt-based confidence gating (SKIP).

Output: A fully working `agent/src/agent/prompt.py` that passes practice mode testing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bilingual-foundation/01-CONTEXT.md
@.planning/phases/01-bilingual-foundation/01-RESEARCH.md

@agent/src/agent/prompt.py
@agent/src/agent/__main__.py
@core/src/core/frame.py

<interfaces>
<!-- Frame contract from core/src/core/frame.py -->
```python
@dataclass(frozen=True)
class Frame:
    image: Image.Image  # PIL Image (RGB)
    timestamp: datetime  # UTC
```

<!-- Calling convention from agent/src/agent/__main__.py -->
```python
# Practice mode (line 56): imports analyze, calls per frame
from agent.prompt import analyze
# ...
async for frame in start_practice(camera_index=camera, fps=fps):
    guess = await analyze(frame)
    if guess:
        print(f"  [guess] {guess}")
    else:
        print("  [skip]  No guess this frame")
```

<!-- pydantic-ai BinaryContent API (from research) -->
```python
from pydantic_ai import Agent, BinaryContent

# Agent.run() accepts str | Sequence[UserContent]
# UserContent includes str and BinaryContent
result = await agent.run([
    "text prompt",
    BinaryContent(data=jpeg_bytes, media_type="image/jpeg"),
])
answer = result.output  # str
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement complete bilingual charades agent</name>
  <files>agent/src/agent/prompt.py</files>
  <read_first>
    - agent/src/agent/prompt.py (current placeholder to replace)
    - agent/src/agent/__main__.py (calling convention -- DO NOT EDIT)
    - core/src/core/frame.py (Frame dataclass contract)
    - .planning/phases/01-bilingual-foundation/01-RESEARCH.md (verified patterns and code examples)
    - .planning/phases/01-bilingual-foundation/01-CONTEXT.md (locked decisions D-01 through D-18)
  </read_first>
  <action>
Replace the entire contents of `agent/src/agent/prompt.py` with a working implementation. The file must contain these exact components:

**1. Imports (top of file):**
```python
from __future__ import annotations
import io
from pydantic_ai import Agent, BinaryContent
from PIL import Image
from core import Frame
```

**2. SYSTEM_PROMPT constant (per D-07, D-08, D-09, D-10, FOUND-05, LANG-02, LANG-04):**
A multi-line string that includes ALL of the following (exact wording is Claude's discretion per CONTEXT.md):
- Charades framing: "A person is acting out or miming a concept" (per D-07)
- Anti-hallucination: "The person is MIMING and does NOT have real objects" (per FOUND-05, Pitfall 5 from research)
- Focus instruction: "Focus on gestures, body language, and movements, NOT the background" (per D-07)
- Bilingual instruction: "Answers may be in English or Tagalog" (per D-08, LANG-03)
- Pinoy Henyo categories with all 5 listed: Bagay (Things), Tao (People), Lugar (Places), Pangyayari (Events), Pagkain (Food) (per D-08, LANG-02)
- Filipino reference list with at least these items: adobo, sinigang, halo-halo, balut, taho, jeepney, tricycle, tinikling, karaoke, Boracay, Intramuros (per D-10, LANG-04)
- Output format: "Give your best guess as a short phrase (1-5 words)" (per D-09, LANG-01)
- SKIP instruction: "If you are not confident enough, respond with exactly SKIP" (per D-09, D-11)
- Specificity instruction: encourage specific guesses over generic ones (per D-16)

**3. Module-level Agent (per D-01, D-02):**
```python
_agent = Agent("google-gla:gemini-2.5-flash", system_prompt=SYSTEM_PROMPT)
```
Agent MUST be at module level, NOT inside analyze(). This avoids cold start penalty per Pitfall 12 from research.

**4. Module-level state dict (per D-13, D-14, FOUND-02):**
```python
_state: dict = {
    "frame_count": 0,
    "guess_history": [],
    "last_guess": None,
}
```

**5. _preprocess_frame helper (per D-04, D-05, D-06, FOUND-04):**
A private function `_preprocess_frame(image: Image.Image) -> BinaryContent` that:
- Copies the image first with `image.copy()` (Pitfall 2: thumbnail modifies in-place)
- Calls `img.thumbnail((512, 512), Image.LANCZOS)` to resize preserving aspect ratio (per D-04)
- Calls `img.convert("RGB")` defensively before JPEG save (Pitfall 3: RGBA fails)
- Saves to BytesIO as JPEG quality=70 (per D-05)
- Returns `BinaryContent(data=buf.getvalue(), media_type="image/jpeg")`
- No additional preprocessing per D-06

**6. analyze() function (per FOUND-01, FOUND-03, D-12, D-17, D-18):**
`async def analyze(frame: Frame) -> str | None` that:
- Increments `_state["frame_count"]`
- Calls `_preprocess_frame(frame.image)` to get BinaryContent
- Calls `await _agent.run([user_prompt_text, binary_content])` where user_prompt_text asks what the person is acting out
- Strips whitespace AND quotes from output: `result.output.strip().strip('"').strip("'")`  (per D-18)
- Returns None if stripped answer is "SKIP" (case-insensitive comparison via `.upper() == "SKIP"`) (per D-12, FOUND-03)
- Otherwise updates `_state["last_guess"]` and appends to `_state["guess_history"]` (per D-14)
- Returns the answer string

**Important anti-patterns to AVOID:**
- Do NOT create Agent inside analyze() -- module level only (Pitfall 12)
- Do NOT store raw PIL Images in state -- only store text (Pitfall 13)
- Do NOT use Image.resize() -- use Image.thumbnail() for aspect ratio preservation
- Do NOT wrap output in JSON or structured format (per D-17) -- plain text guess
- Do NOT add any preprocessing beyond resize+JPEG (per D-06)

Keep the file's original module docstring or write a brief replacement that explains the file's purpose.
  </action>
  <verify>
    <automated>cd /Users/adelavega/Developer/build-day-manila-2026-starter-pack && python -c "
import ast, sys
with open('agent/src/agent/prompt.py') as f:
    source = f.read()
tree = ast.parse(source)
names = {node.name for node in ast.walk(tree) if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef))}
assigns = {node.targets[0].id for node in ast.walk(tree) if isinstance(node, ast.Assign) and isinstance(node.targets[0], ast.Name)}
# Check required components exist
assert 'analyze' in names, 'Missing analyze function'
assert '_preprocess_frame' in names, 'Missing _preprocess_frame function'
assert 'SYSTEM_PROMPT' in assigns, 'Missing SYSTEM_PROMPT constant'
assert '_agent' in assigns, 'Missing _agent module-level Agent'
assert '_state' in assigns, 'Missing _state dict'
# Check key content in source
assert 'google-gla:gemini-2.5-flash' in source, 'Missing Gemini model string'
assert 'BinaryContent' in source, 'Missing BinaryContent import'
assert 'thumbnail' in source, 'Missing thumbnail call'
assert 'SKIP' in source, 'Missing SKIP handling'
assert 'Tagalog' in source or 'tagalog' in source, 'Missing Tagalog reference'
assert 'Pinoy Henyo' in source or 'Bagay' in source, 'Missing Pinoy Henyo categories'
assert 'adobo' in source, 'Missing Filipino food reference'
assert 'jeepney' in source, 'Missing Filipino transport reference'
assert 'tinikling' in source, 'Missing Filipino cultural reference'
assert 'miming' in source.lower() or 'acting out' in source.lower(), 'Missing charades framing'
assert 'frame_count' in source, 'Missing frame_count in state'
assert 'guess_history' in source, 'Missing guess_history in state'
assert 'last_guess' in source, 'Missing last_guess in state'
print('ALL CHECKS PASSED')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - File contains `SYSTEM_PROMPT` string with "miming" or "acting out" charades framing
    - File contains `_agent = Agent("google-gla:gemini-2.5-flash"` at module level
    - File contains `_state` dict with keys "frame_count", "guess_history", "last_guess"
    - File contains `_preprocess_frame` function using `thumbnail((512, 512)` and `convert("RGB")` and `format="JPEG", quality=70`
    - File contains `async def analyze(frame: Frame) -> str | None` that calls `_agent.run` with BinaryContent
    - SKIP detection is case-insensitive: `.upper() == "SKIP"`
    - Output is stripped of whitespace and quotes before SKIP check
    - SYSTEM_PROMPT contains all 5 Pinoy Henyo categories (Bagay, Tao, Lugar, Pangyayari, Pagkain)
    - SYSTEM_PROMPT contains at least: adobo, sinigang, halo-halo, balut, taho, jeepney, tricycle, tinikling, karaoke, Boracay, Intramuros
    - SYSTEM_PROMPT contains "1-5 words" output format instruction
    - SYSTEM_PROMPT contains SKIP instruction
    - image.copy() is called before thumbnail() to avoid mutating Frame
    - No Agent instantiation inside analyze() (only at module level)
  </acceptance_criteria>
  <done>
    `agent/src/agent/prompt.py` contains a complete bilingual charades agent with: Gemini 2.5 Flash via pydantic-ai, PIL preprocessing (512px thumbnail + JPEG q70), Filipino cultural context in system prompt, SKIP-based confidence gating, and module-level state tracking. The automated verification script passes with "ALL CHECKS PASSED".
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify agent works in practice mode</name>
  <files>agent/src/agent/prompt.py</files>
  <read_first>
    - agent/src/agent/prompt.py (just implemented)
  </read_first>
  <action>
Human verification checkpoint. The executor should present the verification steps to the user and wait for confirmation. No code changes needed -- this task validates that the implementation from Task 1 works end-to-end.

Pre-check: Confirm GEMINI_API_KEY is set in .env. If not, instruct the user to get a key from https://aistudio.google.com/apikey and add `GEMINI_API_KEY=your-key` to the .env file.
  </action>
  <what-built>Complete Phase 1 bilingual charades agent: Gemini 2.5 Flash integration, frame preprocessing, bilingual system prompt with Filipino cultural context, SKIP confidence gating, and state tracking across frames.</what-built>
  <how-to-verify>
    1. Ensure GEMINI_API_KEY is set in your .env file (get a key from https://aistudio.google.com/apikey if needed)
    2. Run: `uv run -m agent --practice`
    3. Point your camera at objects, people, or act out simple charades
    4. Verify these behaviors:
       - Agent outputs text guesses (not "No LLM configured yet")
       - Some frames produce [guess] output (agent is guessing)
       - Some frames produce [skip] output (agent is gating confidence)
       - Guesses are short phrases (1-5 words), not paragraphs
       - No crashes or import errors
    5. (Optional) Run: `uv run -m agent --practice --fps 2` to test at higher frame rate
  </how-to-verify>
  <verify>
    <automated>echo "Checkpoint: requires human verification via practice mode"</automated>
  </verify>
  <acceptance_criteria>
    - Practice mode runs without crashing
    - Agent produces at least one [guess] output within 30 seconds
    - Agent produces at least one [skip] output (not guessing every single frame)
    - Guesses are readable short phrases, not error messages or structured data
  </acceptance_criteria>
  <done>User has confirmed the agent works in practice mode: produces guesses, demonstrates SKIP behavior, and runs without errors.</done>
  <resume-signal>Type "approved" if the agent works, or describe any issues you see</resume-signal>
</task>

</tasks>

<verification>
Phase 1 verification checklist:
1. `agent/src/agent/prompt.py` has been replaced with working implementation
2. Automated AST checks confirm all required components exist
3. Practice mode runs and produces guesses from Gemini 2.5 Flash
4. Agent demonstrates SKIP behavior (not guessing on every frame)
5. System prompt contains full bilingual Filipino context
</verification>

<success_criteria>
- FOUND-01: Agent sends preprocessed frames to Gemini 2.5 Flash and receives text guesses
- FOUND-02: Module-level _state dict persists frame_count, guess_history, last_guess across calls
- FOUND-03: Agent returns None when LLM outputs SKIP (case-insensitive)
- FOUND-04: Frames resized to 512px via thumbnail() and JPEG-compressed at quality 70
- FOUND-05: System prompt says "miming/acting out" not "what do you see"
- LANG-01: Output format instructs 1-5 word phrases
- LANG-02: Pinoy Henyo categories (Bagay/Tao/Lugar/Pangyayari/Pagkain) in prompt
- LANG-03: Prompt instructs "English or Tagalog" answers
- LANG-04: Filipino references (adobo, jeepney, tinikling, Boracay, etc.) in prompt
</success_criteria>

<output>
After completion, create `.planning/phases/01-bilingual-foundation/01-01-SUMMARY.md`
</output>
